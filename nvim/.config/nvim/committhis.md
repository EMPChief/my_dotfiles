- Remove vim-autoread plugin (astrocore.lua already handles autoread via native autocmds)
- Fix <Leader>se mapping: was incorrectly calling resize+5 twice; now uses <C-w>= to equalize splits
- Remove invalid textobjects = { enable = true } from treesitter config (not a valid nvim-treesitter option; textobjects requires a separate plugin)
- Remove dead BufWritePre autocmd from formatting.lua (only returned early, never called conform; format_on_save in conform.setup handles this)
- Remove lazy = false from vim_tmux_navigator (redundant when keys spec is defined)
- Add noice route to suppress treesitter dead-node error on Neovim 0.12 until upstream fix lands
- Remove empty on_attach function from astrolsp.lua
- Fix astroui.lua: remove duplicate colorscheme = "astrodark" (overridden by tokyonight.lua)
- Rename none-ls.lua → lsp.lua (file configures nvim-lspconfig, not none-ls)
- Rename persistance.lua → persistence.lua (typo fix)
- Move opencode keymaps from <leader>o* to <leader>ai* to avoid conflicts with obsidian's <leader>o\* mappings
